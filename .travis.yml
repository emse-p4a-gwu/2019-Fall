language: R
sudo: false
cache: packages
cran:
  http://lib.stat.cmu.edu/R/CRAN/
script:
- R -e "rmarkdown::render_site('.')"
- touch _site/.nojekyll
deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  local_dir: _site
  on:
    branch: master
